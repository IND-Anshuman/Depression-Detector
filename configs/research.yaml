defaults:
  - base

mode: research

dataset:
  name: depvidmood_feature_manifest
  manifest_csv: data/manifests/depvidmood_feature_manifest.csv

features:
  backend: mediapipe_full
  include_emonet: true
  include_entropy: true

training:
  epochs: 50
  use_wandb: true
